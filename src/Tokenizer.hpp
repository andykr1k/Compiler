#include "Token.hpp"

vector<Token> Tokenize(const string& str){
    vector<Token> tokens;
    
    for (int i = 0; i < str.length(); i++) {
        
        cout << str[0] << endl;
    }

    return tokens;
}

